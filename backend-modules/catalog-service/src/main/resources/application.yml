spring.application.name: catalog-service

spring:
  cloud:
      ignoredInterfaces:
        - lo*
        
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8001/eureka/