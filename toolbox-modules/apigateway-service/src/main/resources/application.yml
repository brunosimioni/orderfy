spring:
  application:
    name: apigateway-service
  cloud:
    ignoredInterfaces:
      - lo*

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8001/eureka